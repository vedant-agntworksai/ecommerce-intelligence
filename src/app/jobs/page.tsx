import { getDb } from "@/lib/db"; import { JobRepository } from "@/lib/db/repositories/job-repository";
export default async function Page(){let jobs:any[]=[];try{jobs=await new JobRepository(await getDb()).recent(50);}catch{}
return <div className="p-8"><p className="text-sm text-cyan-400">Scrape Jobs</p><h2 className="text-3xl font-semibold mt-1">Collection queue</h2><p className="muted mt-2 mb-6">Background work for discovery, PDPs, reviews, matching, and competitor expansion.</p><div className="card overflow-hidden"><table className="w-full text-sm"><thead><tr className="text-left text-slate-400">{["Created","Type","Status","Error"].map(x=><th key={x} className="p-4">{x}</th>)}</tr></thead><tbody>{jobs.map(j=><tr key={j.id} className="border-t border-slate-800"><td className="p-4">{String(j.created_at)}</td><td>{j.type}</td><td>{j.status}</td><td className="text-rose-300">{j.error??"—"}</td></tr>)}{!jobs.length&&<tr><td colSpan={4} className="p-10 text-center muted">No jobs yet.</td></tr>}</tbody></table></div></div>}